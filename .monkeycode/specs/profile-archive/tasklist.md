# 制度档案页面实施计划

- [x] 1. 扩展存档Schema与类型定义
   - 在 `app/types/game.ts` 中新增 `ProfileHistoryEntry` 接口
   - 在 `useGameStorage.ts` 中更新存档类型定义，添加 `profileHistory: ProfileHistoryEntry[]` 字段

- [x] 2. 实现画像历史记录逻辑
   - [x] 2.1 在 `useGameComputed.ts` 中扩展 `refreshProfileSnapshot()` 方法
     - 添加画像变更检测逻辑（对比上一次快照）
     - 画像变化时创建 ProfileHistoryEntry 并追加到 history 数组
     - 限制历史记录最多 50 条

   - [x] 2.2 在 `app/logic/gameEngine.ts` 中新增风险评估函数
     - 实现 `calculateRiskScore(profile: SocialProfile): number` 加权计算
     - 实现 `generateSystemNote(profile: SocialProfile): string` 评语生成

   - [x] 2.3 在 `useGameStorage.ts` 中添加旧档迁移逻辑
     - 加载存档时检查 `profileHistory` 字段
     - 如果不存在则初始化为空数组
     - 如果有 `profileSnapshot` 但无历史，自动创建第一条记录

- [x] 3. 检查点 - 确保所有测试通过
  - 确保所有测试通过，如有疑问请询问用户

- [x] 4. 创建档案页面基础结构
   - [x] 4.1 创建 `app/pages/profile.vue` 页面容器
     - 使用 Nuxt 页面布局
     - 添加返回游戏的导航按钮
     - 从 `useGame()` 读取 `profileSnapshot` 和 `profileHistory`

   - [x] 4.2 在主游戏页面 Header 添加「档案」入口按钮
     - 在 `app/pages/game.vue` 的 Header 区域新增按钮
     - 点击后导航到 `/profile` 路由

- [x] 5. 实现核心展示组件（整合到 profile.vue 中）
   - [x] 5.1 风险评分仪表盘
     - 综合风险评分圆形仪表盘（conic-gradient）
     - 四维画像等级卡片（财务风险、教育信用、制度顺从、身体资产）
     - 每个维度显示等级名称和进度条
     - 底部显示系统评语

   - [x] 5.2 标签时间线
     - 垂直时间轴布局，最新记录在顶部
     - 每个条目显示日期、触发原因、标签列表
     - 标签颜色按类型区分（红/蓝/绿/紫）
     - 支持收起/展开，默认显示最近5个

   - [x] 5.3 评估历史表格
     - 表格形式展示历史评估快照
     - 列：日期、触发原因、风险分、主要维度、系统备注
     - 风险分颜色编码

- [x] 6. 实现空状态与错误处理
   - [x] 6.1 创建空状态组件 `app/components/profile/EmptyState.vue`
     - 无历史记录时显示「档案建立中...」
     - 添加「数据采集中」动画（脉冲环 + SVG 图标动画）
     - 底部提示文案

   - [x] 6.2 页面布局优化
     - 内容居中展示（.archive-wrapper 使用 flex + align-items: center）
     - 赛博朋克深色主题（#05060A 背景 + 网格线效果）
     - 响应式适配（桌面/平板/移动端）

- [x] 7. 检查点 - 确保所有测试通过
  - 确保所有测试通过，如有疑问请询问用户

- [x] 8. 添加CSS样式与主题
   - [x] 8.1 在 `app/pages/profile.vue` 中实现完整样式
     - 制度档案风格（严肃、正式、赛博朋克美学）
     - 表格样式、时间轴样式、卡片样式
     - 响应式适配（桌面/平板/移动端）

   - [x] 8.2 实现标签颜色系统
     - 财务风险标签：红色系
     - 教育信用标签：蓝色系
     - 制度顺从标签：绿色系
     - 身体资产标签：紫色系

- [ ]* 9. 编写单元测试
   - [ ]* 9.1 为 `calculateRiskScore()` 编写测试
     - 测试各维度不同等级的分数计算
     - 测试边界情况（空画像、极端值）

   - [ ]* 9.2 为 `generateSystemNote()` 编写测试
     - 测试不同标签组合的评语生成
     - 测试空标签情况

   - [ ]* 9.3 为画像历史记录逻辑编写测试
     - 测试画像变更检测
     - 测试历史记录上限（50条）
     - 测试旧档迁移逻辑

   - [ ]* 9.4 为档案页面组件编写测试
     - 测试空状态渲染
     - 测试有数据时的完整展示
     - 测试错误边界处理

- [ ] 10. 最终检查点 - 确保所有测试通过
  - 确保所有测试通过，如有疑问请询问用户

- [x] 4. 创建档案页面基础结构
   - [x] 4.1 创建 `app/pages/profile.vue` 页面容器
     - 使用 Nuxt 页面布局
     - 添加返回游戏的导航按钮
     - 从 `useGame()` 读取 `profileSnapshot` 和 `profileHistory`

   - [x] 4.2 在主游戏页面 Header 添加「档案」入口按钮
     - 在 `app/pages/game.vue` 的 Header 区域新增按钮
     - 点击后导航到 `/profile` 路由

- [x] 5. 实现核心展示组件
   - [x] 5.1 创建 `app/pages/profile.vue` 完整页面（整合所有组件）
     - 显示标题「修仙信用社会 · 制度档案」
     - 显示档案编号（基于玩家ID生成）和建档日期
     - 返回游戏按钮
     - 风险评分仪表盘
     - 身份摘要卡片
     - 系统评语

   - [x] 5.2 实现风险评分展示
     - 展示四维画像等级卡片（财务风险、教育信用、制度顺从、身体资产）
     - 每个维度显示等级名称和进度条
     - 综合风险评分圆形仪表盘（conic-gradient）
     - 底部显示系统评语

   - [x] 5.3 实现标签时间线
     - 实现垂直时间轴布局
     - 每个标签项显示名称、获取时间、触发原因
     - 标签颜色按类型区分（红/蓝/绿/紫）
     - 支持收起/展开，默认显示最近5个

   - [x] 5.4 实现评估历史表格
     - 实现表格形式展示历史评估快照
     - 列：日期、触发原因、风险分、主要维度、系统备注
     - 风险分颜色编码

- [x] 6. 实现空状态与错误处理
   - [x] 6.1 创建空状态组件 `app/components/profile/EmptyState.vue`
     - 无历史记录时显示「档案建立中...」
     - 添加「数据采集中」动画（脉冲环 + SVG 图标动画）
     - 底部提示文案

   - [x] 6.2 页面布局优化
     - 内容居中展示（max-width: 1200px, margin: 0 auto）
     - 赛博朋克深色主题（#05060A 背景 + 网格线效果）
     - 响应式适配（桌面/平板/移动端）

- [x] 7. 检查点 - 确保所有测试通过
  - 确保所有测试通过，如有疑问请询问用户

- [x] 8. 添加CSS样式与主题
   - [x] 8.1 在 `app/pages/profile.vue` 中实现完整样式
     - 制度档案风格（严肃、正式、赛博朋克美学）
     - 表格样式、时间轴样式、卡片样式
     - 响应式适配（桌面/平板/移动端）

   - [x] 8.2 实现标签颜色系统
     - 财务风险标签：红色系
     - 教育信用标签：蓝色系
     - 制度顺从标签：绿色系
     - 身体资产标签：紫色系

- [ ]* 9. 编写单元测试
   - [ ]* 9.1 为 `calculateRiskScore()` 编写测试
     - 测试各维度不同等级的分数计算
     - 测试边界情况（空画像、极端值）

   - [ ]* 9.2 为 `generateSystemNote()` 编写测试
     - 测试不同标签组合的评语生成
     - 测试空标签情况

   - [ ]* 9.3 为画像历史记录逻辑编写测试
     - 测试画像变更检测
     - 测试历史记录上限（50条）
     - 测试旧档迁移逻辑

   - [ ]* 9.4 为档案页面组件编写测试
     - 测试空状态渲染
     - 测试有数据时的完整展示
     - 测试错误边界处理

- [ ] 10. 最终检查点 - 确保所有测试通过
  - 确保所有测试通过，如有疑问请询问用户
